# Week # 2 of DSA, Assignment # 1
## Q1. Push and Pop
Create a stack and perform these operations:
Push 10
Push 20
Push 30
Pop
Push 40
Pop
Print the stack after each operation.
## Q2. Find the Top
Given:
stack = [10, 20, 30, 40, 50]
Write code to print the top element without removing it.
## Q3. Stack Size
Given:
stack = [5, 10, 15, 20]
Write code to find the number of elements in the stack.
## Q4. Check Empty
Write a program that checks whether a stack is empty.
Test it with:
stack = [ ]
and:
stack = [10, 20]
## Q5. Predict the Output
Without running the code, predict the output:
stack = [ ]

stack.append(10)
stack.append(20)
stack.append(30)

print(stack.pop())
print(stack.pop())

stack.append(40)

print(stack)
print(stack[-1])

## Q6. LIFO
What will be the output?
stack = [ ]

stack.append("A")
stack.append("B")
stack.append("C")

print(stack.pop())
print(stack.pop())
print(stack.pop())
Explain why the output comes in that order.
## Q7. Stack Operations
Starting with:
stack = [ ]
Perform the following:
Push 5
Push 10
Push 15
Pop
Push 20
Push 25
Pop
At the end, print:
The final stack
The top element
The size of the stack
Whether the stack is empty
##Q8. Small Coding Challenge
Write a program that takes 5 numbers from the user, pushes them into a stack, and then pops and prints all 5 numbers.


